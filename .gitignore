# Dependencies
node_modules/

# Environment variables
.env
.env.*
!/.env.example

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Build output
dist/
build/

# OS generated files
.DS_Store
Thumbs.db

# Data files that might be dynamically generated or sensitive
# Если вы хотите, чтобы news.json всегда начинался с пустого или определенного состояния из репозитория,
# то не добавляйте его в .gitignore.
# Если news.json управляется только приложением и может содержать пользовательские данные,
# которые не должны быть в git, добавьте его.
# Учитывая эфемерность файловой системы Vercel, для news.json это менее критично,
# но для локальной разработки может быть полезно.
# data/news.json